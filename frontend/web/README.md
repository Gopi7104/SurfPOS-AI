# web/ (Flutter Platform Runner)

**Status: scaffolded** — generated via `flutter create --platforms=web .`, used for local `flutter analyze`/`flutter test`/build verification during the design-system build (see [docs/09_PROMPT_HISTORY.md](../../docs/09_PROMPT_HISTORY.md)). Note: `flutter build web` currently fails on the primary dev machine due to a local Windows Application Control policy blocking Flutter's shader-compiler binary — see [.claude/projectStatus.md § Known Issues #5](../../.claude/projectStatus.md#known-issues), not a code defect.
