# ios/ (Flutter Platform Runner)

**Status: scaffolded** — generated via `flutter create --platforms=ios .` so `flutter_launcher_icons` had a real `Assets.xcassets/AppIcon.appiconset/` to write into (see [docs/09_PROMPT_HISTORY.md](../../docs/09_PROMPT_HISTORY.md)). This is the standard Flutter-generated iOS/Xcode project — nothing hand-written. Cannot be built/run from this Windows dev machine (no Xcode); a Mac is required for that, same as any Flutter iOS work.
