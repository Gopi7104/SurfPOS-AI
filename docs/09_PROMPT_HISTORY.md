# 09 — Prompt History

> This file is Claude's memory of *why* the codebase looks the way it does, across sessions. **Every future Claude Code session that makes a non-trivial change must append an entry here** before finishing. Read alongside [13_CLAUDE_CONTEXT.md](13_CLAUDE_CONTEXT.md) and [11_CHANGELOG.md](11_CHANGELOG.md) (this file explains the *why*; the changelog records the *what*, user-facing).

---

## How to Add an Entry

Append a new entry at the **top** of the log (§2), directly under this heading, using the template below. Do not edit or delete past entries — this is a historical record.

```markdown
### YYYY-MM-DD — <short title>

- **Prompt Summary:** What the user asked for, in 1–3 sentences.
- **Files Changed:** List of files/folders touched.
- **Reason:** Why this work was requested (business/technical motivation).
- **Decision:** Any decision made while doing the work (link a new/updated ADR in [08_ARCHITECTURE_DECISIONS.md](08_ARCHITECTURE_DECISIONS.md) if applicable).
```

---

## Log

### 2026-07-30 — Firebase Infrastructure Connection

- **Prompt Summary:** Connect Firebase end-to-end to both the Flutter frontend (packages, `main.dart` init, Android Gradle wiring, no UI changes) and the Express backend (secure credential loading, Admin SDK init, Auth/RTDB/Storage config, env validation) — explicitly no business features, no changes to Merchant/Store/Inventory/Billing/Surfboard SDK. User then provided a real Firebase service-account JSON (project `surfpos-ai`) placed in `backend/src/config/`.
- **Files Changed:** New: `backend/scripts/verifyFirebaseConnection.js`. Modified: `backend/src/firebase/admin.js` (per-service validation, firebase-admin v13+ modular API fix), `backend/src/config/index.js` (`FIREBASE_STORAGE_BUCKET`, test/prod env-file isolation), `backend/src/constants/environmentKeys.js`, `backend/.env` (new, gitignored, real credentials), `backend/.env.example`, `backend/package.json` (+`verify:firebase`), `.gitignore` (fixed service-account pattern), `frontend/pubspec.yaml` (+firebase_core/firebase_auth), `frontend/lib/main.dart` (Firebase.initializeApp()), `frontend/android/{settings.gradle.kts,app/build.gradle.kts}` (google-services plugin). Removed: the raw service-account JSON file (extracted into `.env`, redundant/riskier to leave in place). Docs: `README.md`, `docs/10_TASKS.md`, `docs/11_CHANGELOG.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-025).
- **Reason:** P.1 (Firebase project setup) had blocked real end-to-end verification since Phase 1 — every Repository/service was correct in isolation (DI + fakes) but never proven against an actual Firebase project.
- **Decision:** Recorded as [ADR-025](08_ARCHITECTURE_DECISIONS.md#adr-025--firebase-infrastructure-connection-real-credentials-modular-api-migration-testprod-isolation). Before writing code, asked the user how to proceed given no `google-services.json`/backend credentials/Firebase CLI existed in this environment — the user then supplied the backend service-account JSON directly. First action on receiving it: fixed `.gitignore`'s Firebase pattern, which didn't match Firebase Console's real filename shape and left the key unprotected from being committed. Extracting it into `backend/.env` (the existing, correct mechanism) and deleting the raw JSON surfaced two real, previously-invisible bugs when the code finally ran against real credentials instead of fakes: (1) `firebase-admin@^14.2.0`'s modular API broke every `admin.credential.cert()`/`app.auth()`/`app.database()`/`app.storage()` call — fixed to the v13+ shape; (2) `config/index.js` loaded real `.env` credentials during `npm test` regardless of `NODE_ENV`, and a route-level smoke test (built in Phase 3 under the assumption Firebase was always unconfigured in tests) actually created a real user in the live project before this was caught — fixed by loading `.env.test` (absent, so nothing loads) whenever `NODE_ENV=test`, and the stray user was found (`getUserByEmail`) and deleted with the user's explicit confirmation. Verified honestly rather than claimed: `npm run verify:firebase` initially showed Auth passing with RTDB/Storage correctly reporting "not configured" since no database URL or bucket name were provided — nothing was guessed. `flutter build apk --debug` was run and, as expected, fails cleanly at the `google-services` Gradle plugin step with a specific "file is missing" error, confirming the Gradle wiring itself is correct and the only blocker is the absent `google-services.json`. Confirmed via `git status`/file review that no Merchant/Store/Inventory/Billing/Surfboard SDK file was touched. **Follow-up same session:** the user filled in a real `FIREBASE_DATABASE_URL` directly in `.env` and explicitly asked to skip Storage for now. Re-running `verify:firebase` surfaced one more bug — the script never exited (RTDB holds a persistent connection open, so the one-shot CLI hung after printing its results) — fixed with an explicit `process.exit()` in a `.finally()`. Re-verified: Auth **and** Realtime Database both now genuinely pass end-to-end against the real project; Storage still correctly reports "not configured" (deliberately deferred, not a gap).

### 2026-07-29 — Phase 7: Inventory Management

- **Prompt Summary:** With Phase 6 approved, the user asked for Roadmap Phase 7 — Inventory only, entirely Firebase-owned (explicitly no Surfboard communication at all), covering Products (name/category/barcode/SKU/supplier/price/cost price/tax/unit/minimum stock/status) and per-store stock, with search/filtering/pagination/soft-delete, Controller → Service → Repository (Firebase only, no SDK) layering, DI, and unit tests covering CRUD/stock/search/pagination/soft-delete/validation/repository behavior.
- **Files Changed:** New: `backend/src/modules/inventory/{inventory.service,product.repository,stock.repository}.js`, `backend/src/validators/inventory.validation.js`, `backend/src/controllers/inventory.controller.js`, `backend/src/routes/inventory.routes.js`, `backend/tests/modules/inventory/{inventory.service,product.repository,stock.repository}.test.js`, `backend/tests/inventory.test.js`. Modified: `backend/src/utils/errors.js` (+`InsufficientStockError`), `backend/src/modules/store/store.service.js` (+`verifyStoreOwnership` export), `backend/src/app.js` (mounts `/inventory`), `backend/src/constants/messages.js`, `backend/src/modules/inventory/README.md`. Docs: `docs/10_TASKS.md`, `docs/11_CHANGELOG.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-024), `docs/04_API_DOCUMENTATION.md`, `docs/03_DATABASE_DESIGN.md`.
- **Reason:** A merchant needs a real product catalog and per-location stock tracking before Billing (Phase 8) can validate a cart against real inventory — this is the first phase with genuinely zero Surfboard involvement, a useful checkpoint that the Controller→Service→Repository layering works cleanly without an Integration Client in the mix at all.
- **Decision:** Recorded as [ADR-024](08_ARCHITECTURE_DECISIONS.md#adr-024--inventory-management-in-memory-search--transactional-stock-phase-7). `products/{merchantId}/{productId}` and `inventory/{storeId}/{productId}` were already documented from an earlier documentation pass (predating any implementation), so this phase implemented against that existing schema rather than inventing a new one — just adding one new optional field, `supplierId`, referencing the already-documented-but-unbuilt Supplier entity. Search/filter/pagination are done in-memory over the merchant's full catalog rather than RTDB range queries, a deliberate, documented simplification given the target small-retailer catalog size and RTDB's weaker query story compared to Firestore. Stock adjustment reuses and extends the exact transaction pattern already shown as a worked example in `docs/21_BACKEND_GUIDELINES.md § 4` — extended to upsert a store's first stock record on initial restock, and to abort (never letting quantity go negative) via a new `InsufficientStockError`, which `07_CODING_RULES.md § 7` had already anticipated by name but never implemented. `merchantId`/`storeId` ownership resolution reuses the exact cross-module-Service pattern from Phases 5–6 (`merchantService.getMerchantId()`, `storeService.verifyStoreOwnership()` — a new export added this pass) rather than reaching into either module's Repository. Barcode lookup (task 7.3) was folded into `GET /inventory/products?barcode=` rather than a separate endpoint, since the user's suggested endpoint list didn't include one and the filter already serves the need. Confirmed via `git status`/file review that no Billing/Payments/Device/Branding/Analytics/AI/Invoice-Scanning code path was touched, and no Surfboard SDK call was made anywhere in this module.

### 2026-07-29 — Phase 6: Store Capabilities

- **Prompt Summary:** With Phase 5 approved, the user asked for Roadmap Phase 6 — Store Management only (create/get/update/list a merchant's stores), explicitly no Inventory/Billing/Payments/Device/Branding/Analytics/AI, Controller → Service → (Repository + Surfboard SDK) layering, DI, "do not invent Surfboard endpoints or payloads — create an interface, mark it a placeholder, document the assumption" repeated even more explicitly than Phase 5's version, and unit tests covering create/get/update/list/validation/Surfboard-failure/repository behavior.
- **Files Changed:** New: `backend/src/modules/store/{store.service,store.repository}.js`, `backend/src/validators/store.validation.js`, `backend/src/controllers/store.controller.js`, `backend/src/routes/store.routes.js`, `backend/src/modules/store/README.md`, `backend/tests/integrations/surfboard/{store.client,storeMapper}.test.js`, `backend/tests/modules/store/{store.service,store.repository}.test.js`, `backend/tests/stores.test.js`. Modified: `backend/src/integrations/surfboard/store.client.js` (real `createStore`/`getStore`/`updateStore`, completing the Phase 2 placeholder), `backend/src/modules/merchant/merchant.service.js` (+`getMerchantId` export), `backend/src/app.js` (mounts `/stores`), `backend/src/constants/{apiRoutes,messages}.js`. Docs: `docs/10_TASKS.md`, `docs/11_CHANGELOG.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-023), `docs/04_API_DOCUMENTATION.md`.
- **Reason:** A merchant needs to register and manage the physical/logical locations (Stores) that inventory, sales, and devices will eventually partition by — without SurfPOS ever caching the full Store object (per [20_DOMAIN_MODEL.md § 1](20_DOMAIN_MODEL.md#1-the-ownership-principle)).
- **Decision:** Recorded as [ADR-023](08_ARCHITECTURE_DECISIONS.md#adr-023--store-capabilities-local-registry--no-invented-list-endpoint-phase-6). Two problems repeated from Phase 5, solved the same way: (1) no `users/{uid}` merchant/store reference exists yet, so `merchantId` is resolved via the same fix Phase 5 used — but this time as a proper cross-module Service→Service call (`merchant.service.js#getMerchantId()`, newly exported) rather than reaching into `merchant.repository.js` directly, since `modules/store/` and `modules/merchant/` are genuinely different modules and the cross-module rule (docs/21_BACKEND_GUIDELINES.md § 8) forbids a Service touching another module's Repository. (2) No Surfboard "list stores by merchant" endpoint is confirmed, so — following the explicit "don't invent an endpoint" instruction — `GET /stores` doesn't call one; instead a new Firebase node, `storeReferences/{uid}/{storeId}`, tracks which storeIds SurfPOS itself created (never the full Store object), and `GET /stores` hydrates each with a live `getStore()` call. That same registry doubles as the ownership check for `GET`/`PATCH /stores/:storeId`, making them IDOR-safe by construction rather than by trusting a post-hoc live field comparison. Confirmed via `git status`/file review that no Inventory/Billing/Payments/Device/Branding/Analytics/AI code path was touched.

### 2026-07-29 — Phase 5: Merchant Functions

- **Prompt Summary:** With Phase 4 approved, the user asked for Roadmap Phase 5 — get/update/status for merchant information, explicitly caller-scoped (`GET/PATCH /merchant`, `GET /merchant/status`, no `:merchantId` param), Controller → Service → (Repository + Surfboard SDK) layering, DI, "never assume an undocumented Surfboard endpoint or payload — build a placeholder/interface and document it instead," and unit tests covering retrieval/update/status/validation/Surfboard-failure/repository behavior.
- **Files Changed:** New: `backend/src/modules/merchant/{merchant.service,merchant.repository}.js`, `backend/src/validators/merchant.validation.js`, `backend/src/controllers/merchant.controller.js`, `backend/src/routes/merchant.routes.js`, `backend/tests/modules/merchant/{merchant.service,merchant.repository}.test.js`, `backend/tests/merchant.test.js`. Modified: `backend/src/integrations/surfboard/merchant.client.js` (+`getMerchant`/`updateMerchant`), `backend/src/integrations/surfboard/mappers/merchant.mapper.js` (+`toMerchantProfile`/`toMerchantUpdateWire`), `backend/src/app.js` (mounts `/merchant`, after `/merchant/applications`), `backend/src/constants/{apiRoutes,messages}.js`, `backend/src/modules/merchant/README.md`, `backend/tests/integrations/surfboard/{merchant.client,merchantMapper}.test.js` (extended). Docs: `docs/10_TASKS.md`, `docs/11_CHANGELOG.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-022), `docs/04_API_DOCUMENTATION.md`.
- **Reason:** Merchant owners need to see and correct their business details, and check onboarding/KYC status, without SurfPOS ever caching the full Merchant object (per [20_DOMAIN_MODEL.md § 1](20_DOMAIN_MODEL.md#1-the-ownership-principle)) — every read is live against Surfboard.
- **Decision:** Recorded as [ADR-022](08_ARCHITECTURE_DECISIONS.md#adr-022--merchant-functions-merchantid-resolution--repository-composition-phase-5). The hardest problem this phase surfaced: `GET/PATCH /merchant`'s suggested shape has no `:merchantId` param, meaning the caller's `merchantId` must be resolved server-side — but Phase 4 deliberately never wrote `users/{uid}.merchantId` ([ADR-021](08_ARCHITECTURE_DECISIONS.md#adr-021--merchant-application-tracking-entity-phase-4)). Resolved by reading the caller's own `merchantApplications/{uid}.merchantId` instead (already populated once Surfboard assigns one) — no route param needed, and any user without an assigned merchantId yet gets a clear `404` rather than a confusing failure. Because that same Firebase node is already fully owned by `merchantApplication.repository.js` (Phase 4), the new `merchant.repository.js` this phase's brief asked for does **not** call `getDb()` itself — it composes the existing repository, so there's still exactly one place that touches `merchantApplications/{uid}`, per [21_BACKEND_GUIDELINES.md § 4](21_BACKEND_GUIDELINES.md#4-repository-firebase-owned-entities-only). Per the explicit "don't assume an undocumented Surfboard endpoint" instruction, `GET /merchant/status` does not call a fabricated "status" endpoint — it's a normalized view built on the same `getMerchant()` call, documented as such in both the SDK client's JSDoc and ADR-022, so swapping in a real status endpoint later (if Surfboard's docs reveal one) is a one-call-site change. Confirmed via `git status`/file review that no Store/Inventory/Billing/Payments/Device/Branding/AI code path was touched.

### 2026-07-29 — Phase 4: Merchant Creation (re-scoped to application tracking)

- **Prompt Summary:** With Phase 3 approved and complete, the user asked for Roadmap Phase 4 — but re-scoped it from the originally-documented `POST /auth/register` orchestration to a narrower "create and track a merchant application" resource: `merchantApplications/{uid}` Firebase tracking metadata (`applicationId`, `merchantId` if available, `applicationStatus`, `applicationUrl`, `submittedAt`, `updatedAt`), `POST/GET /merchant/applications` + `GET /merchant/applications/:id`, explicitly no Store Management/Payments/Inventory/Billing/AI, Controller → Service → (Repository + Surfboard SDK) layering, DI, and unit tests covering success/validation-failure/Surfboard-failure/repository-persistence/controller-response.
- **Files Changed:** New: `backend/src/integrations/surfboard/mappers/merchant.mapper.js`, `backend/src/modules/merchant/{merchantApplication.service,merchantApplication.repository}.js`, `backend/src/validators/merchantApplication.validation.js`, `backend/src/controllers/merchantApplication.controller.js`, `backend/src/routes/merchantApplication.routes.js`, `backend/tests/integrations/surfboard/{merchant.client,merchantMapper}.test.js`, `backend/tests/modules/merchant/{merchantApplication.service,merchantApplication.repository}.test.js`, `backend/tests/merchantApplications.test.js`. Modified: `backend/src/integrations/surfboard/merchant.client.js` (real `createMerchant()`, completing the Phase 2 placeholder), `backend/src/integrations/surfboard/client/surfboardClient.base.js` (bug fix — see Decision), `backend/src/app.js` (mounts `/merchant/applications`), `backend/src/constants/{apiRoutes,messages}.js`, `backend/src/modules/merchant/README.md`. Docs: `docs/10_TASKS.md`, `docs/11_CHANGELOG.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-021), `docs/04_API_DOCUMENTATION.md`, `docs/20_DOMAIN_MODEL.md`, `docs/03_DATABASE_DESIGN.md`.
- **Reason:** Tracking a merchant application as its own Firebase-owned resource — distinct from the Surfboard-owned Merchant object — lets SurfPOS surface submission/KYC-onboarding progress (`applicationStatus`, `applicationUrl`) without duplicating Merchant business fields, and without forcing Store creation and `users/{uid}.merchantId` linkage into the same atomic step before either is actually needed.
- **Decision:** Recorded as [ADR-021](08_ARCHITECTURE_DECISIONS.md#adr-021--merchant-application-tracking-entity-phase-4): new `merchantApplications/{uid}` entity and `/merchant/applications` endpoints (not `/auth/register`); `applicationId` defaults to the caller's `uid` when Surfboard's response doesn't supply a distinct one, keeping the ownership-scoped `GET /:id` lookup structurally IDOR-safe without a second index; one application per uid (409 on resubmission, no reject/retry flow yet). While wiring the first real wiring of `app.js` → `integrations/surfboard/`, found and fixed a real bug: `SurfboardBaseClient` eagerly built its `AuthenticationManager` in the constructor, which validates credentials and threw synchronously whenever `SURFBOARD_API_KEY` was unset — invisible until now because no controller had ever required a Surfboard domain client before. Fixed by making it a lazy getter, matching `firebase/admin.js`'s existing lazy-init convention; this is a bug fix to already-shipped Phase 2/ADR-019 infrastructure, not new scope. `merchantApplication.repository.js` was given a DI-injectable `getDb` (unlike Phase 3's `users.repository.js`) specifically so "Repository persistence" is genuinely unit-testable, given the already-documented Vitest/CJS `vi.mock()` limitation (ADR-020) — a deliberate, acknowledged inconsistency rather than a silent one. Route-level tests remain wiring/validation smoke tests for the same reason as Phase 3; full business-logic coverage lives in `merchantApplication.service.test.js` against constructor-injected fakes. Confirmed via `git status`/file review that no Store/Payments/Inventory/Billing/AI code path was touched.

### 2026-07-29 — Phase 3: Application Client Authentication

- **Prompt Summary:** With the Surfboard SDK authentication layer already complete, the user gave explicit approval and asked for Roadmap Phase 3 itself: application-user (merchant owner/staff) authentication — email/password sign-up, sign-in (verify ID token), `GET /auth/me`, sign-out, and the auth middleware — explicitly Firebase-only, no Merchant/Store/Surfboard record creation, no Surfboard SDK changes, Controller → Service → Repository → Firebase layering, DI, and unit tests.
- **Files Changed:** New: `backend/src/modules/auth/{auth.service,users.repository}.js`, `backend/src/validators/auth.validation.js`, `backend/src/controllers/auth.controller.js`, `backend/src/routes/auth.routes.js`, `backend/tests/modules/auth/auth.service.test.js`, `backend/tests/middleware/auth.middleware.test.js`, `backend/tests/auth.test.js`. Modified: `backend/src/middleware/auth.middleware.js` (refactored to a DI factory delegating to `auth.service.verifyToken`, same `req.user` shape/behavior), `backend/src/app.js` (mounts `/auth`), `backend/src/constants/{apiRoutes,messages}.js`, `backend/src/modules/auth/README.md`. Docs: `docs/10_TASKS.md`, `docs/11_CHANGELOG.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-020), `docs/04_API_DOCUMENTATION.md`.
- **Reason:** Merchant Creation (Phase 4) needs an authenticated owner to attach the new Merchant reference to (per [22_DEVELOPMENT_ROADMAP.md](22_DEVELOPMENT_ROADMAP.md)'s "why this order") — this phase builds that identity layer first, deliberately stopping short of any Surfboard-facing orchestration.
- **Decision:** Recorded as [ADR-020](08_ARCHITECTURE_DECISIONS.md#adr-020--application-client-authentication-endpoint-shape-phase-3): introduced `POST /auth/signup` and `POST /auth/login` rather than reusing the already-documented `POST /auth/register` ([04_API_DOCUMENTATION.md § 2](04_API_DOCUMENTATION.md#2-auth--merchant-onboarding)), because that endpoint has a fundamentally different contract (no auth — client already holds a Firebase ID token from a client-side sign-up — and orchestrates Surfboard Merchant/Store creation), which is Phase 4's job, not this one; reusing the name now would create a real contract conflict once Phase 4 lands. Firebase user creation happens directly via Admin SDK `createUser({email,password})` (backend-owned signup, since no Flutter/client Firebase SDK integration exists yet in this repo) rather than assuming the client already created the account. `middleware/auth.middleware.js` was refactored (not left alone) to delegate token verification to `auth.service.verifyToken()` — this was judged to be "fixing/completing" the already-scaffolded placeholder Phase 2 left behind (per [22_DEVELOPMENT_ROADMAP.md](22_DEVELOPMENT_ROADMAP.md)'s own Phase 3 description: "`auth.middleware.js` token verification (already scaffolded)"), not out-of-scope modification, so there's exactly one implementation instead of a duplicate. `req.user` deliberately still carries only `{uid, email, phoneNumber}` from the token, not the full profile/role — resolving role on every authenticated request would add a Firebase read to every single API call; only `GET /auth/me` does that lookup, matching the existing (pre-Phase-3) comment in `user.types.js` that this enrichment is intentionally deferred. A route-level Supertest suite that mocks `firebase/admin.js` via `vi.mock` was attempted and abandoned: the mock did not intercept the nested `require('../../firebase/admin')` call reached through `app.js`'s CJS require graph (same class of Vitest/CJS module-boundary quirk already documented in the Phase 2 entry below) — rather than fight it, route-level tests were rewritten as validation/middleware wiring smoke tests (asserting the deterministic "Firebase Admin SDK is not configured" boundary), while full business-logic coverage lives in `auth.service.test.js` against constructor-injected fakes, per [21_BACKEND_GUIDELINES.md § 11](21_BACKEND_GUIDELINES.md#11-testing). Confirmed via `git status`/file review that no Merchant/Store/Surfboard code path or `src/integrations/surfboard/` file was touched.

### 2026-07-29 — Surfboard SDK Authentication Layer (extends Phase 2, not Roadmap Phase 3)

- **Prompt Summary:** User asked for a "Phase 3: Surfboard Authentication Layer" — explicitly *not* merchant/Firebase authentication, only the backend's mechanism for authenticating to Surfboard's own API (API Key, Bearer Token, future OAuth), built with a strategy pattern so the auth scheme is swappable without changing SDK code, under `src/integrations/surfboard/{auth,provider,cache,middleware}/`, with DI, unit tests, config validation, and secure secret handling.
- **Files Changed:** New: `backend/src/integrations/surfboard/auth/{authStrategy,authConfig,credentialLoader,authenticationManager}.js`, `auth/strategies/{apiKeyStrategy,bearerTokenStrategy,oauthStrategy}.js`, `provider/{tokenProvider,tokenRefreshStrategy}.js`, `cache/tokenCache.js`, `middleware/authentication.middleware.js`; `backend/tests/integrations/surfboard/{tokenCache,tokenRefreshStrategy,tokenProvider,credentialLoader,authConfig,apiKeyStrategy,bearerTokenStrategy,oauthStrategy,authenticationManager,authenticationMiddleware}.test.js` (27 new tests). Modified: `client/surfboardClient.base.js` (awaits the new `attachAuthentication()` pipeline via an injected/default `AuthenticationManager`), `client/surfboardConfig.js` (resolves `authStrategy`/`bearerToken`), `integrations/surfboard/index.js` (exports new pieces), `src/config/index.js` + `src/constants/environmentKeys.js` (+`SURFBOARD_AUTH_STRATEGY`/`SURFBOARD_BEARER_TOKEN`), `backend/.env.example`, `tests/integrations/surfboard/surfboardClient.base.test.js` (+2 strategy-injection cases). Removed: `middleware/auth.middleware.js` and its test (superseded — the Phase 2 placeholder this work was scoped to complete). Docs: this file, `docs/11_CHANGELOG.md`, `docs/10_TASKS.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-019).
- **Reason:** Phase 2 ([ADR-018](08_ARCHITECTURE_DECISIONS.md#adr-018--surfboard-sdk-implementation-choices-phase-2)) deliberately left the auth-header attachment as a single hardcoded placeholder pending confirmed Surfboard docs. Building the *abstraction* now (rather than waiting for real credentials) is possible and valuable because the strategy pattern isolates the still-unconfirmed wire-level specifics to one small file per scheme — exactly the same "isolate the guesswork" approach ADR-018 already used for the webhook signature scheme.
- **Decision:** Before writing any code, cross-checked the user's "Phase 3" framing against `docs/10_TASKS.md`/`docs/22_DEVELOPMENT_ROADMAP.md`, which define the roadmap's actual Phase 3 as **Firebase Auth (merchant/staff sign-in)** — a different, unrelated piece of work that [13_CLAUDE_CONTEXT.md § 5](13_CLAUDE_CONTEXT.md) explicitly gates on user approval and which remains `Not Started`, untouched. Rather than blocking on this naming mismatch, proceeded with the (well-specified, clearly-scoped) Surfboard SDK auth work and recorded it as [ADR-019](08_ARCHITECTURE_DECISIONS.md#adr-019--surfboard-sdk-authentication-layer-strategy-pattern-extends-phase-2-not-roadmap-phase-3) — extending Phase 2, explicitly *not* claiming the roadmap's Phase 3 number, so the two are never conflated in the living docs. Default strategy (`api_key`) reproduces Phase 2's exact prior behavior so no regression occurs for existing callers/tests. `OAuthStrategy` fetches its own token directly (injectable `fetchImpl`, not routed through `SurfboardBaseClient`) to avoid a circular dependency between the auth layer and the request layer it feeds. One test needed adjusting for the same Vitest CJS/ESM module-boundary `instanceof` quirk already documented in the Phase 2 entry below — fixed by asserting on the thrown error's message instead, consistent with that established pattern.

### 2026-07-29 — Phase 2: Surfboard Client SDK

- **Prompt Summary:** With the Surfboard-alignment documentation approved, the user asked to implement *only* Roadmap Phase 2 (Surfboard Client SDK) — real HTTP infrastructure under `src/integrations/surfboard/{client,middleware,models,mappers,utils,errors}/` that every domain client inherits — with explicit instructions not to touch Authentication, Merchant, Store, Payments, Inventory, Billing, or AI, and to stop for approval afterward.
- **Files Changed:** New: `backend/src/integrations/surfboard/client/{surfboardClient.base,surfboardConfig}.js`; `middleware/{retry,timeout,auth,requestLogger,responseLogger}.middleware.js`; `models/{environment,requestOptions,response}.js`; `mappers/baseMapper.js`; `utils/{requestId,requestBuilder,responseParser,webhookSignatureVerifier}.js`; `errors/{surfboardApiError,errorMapper}.js`; `backend/tests/integrations/surfboard/` (9 test files, 48 tests). Modified: `backend/src/integrations/surfboard/{auth,merchant,payment,store,device,branding}.client.js` (require path updated to the new `client/` location — no domain methods added), `index.js` (exports the new SDK pieces), `README.md`; `backend/src/constants/{httpStatus,errorCodes,messages}.js` (added `BAD_GATEWAY`/`SURFBOARD_ERROR`/messages). Removed: the old root-level `surfboardClient.base.js` placeholder (moved into `client/`, fully implemented). Docs: `docs/10_TASKS.md` (Phase 2 marked Done), `docs/22_DEVELOPMENT_ROADMAP.md` (Phase 2 marked Done), `docs/13_CLAUDE_CONTEXT.md`, `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-018), `docs/11_CHANGELOG.md`.
- **Reason:** Every phase from 3 onward needs a working Surfboard integration layer underneath it (per [22_DEVELOPMENT_ROADMAP.md](22_DEVELOPMENT_ROADMAP.md)'s "why this order" — Phase 2 is a hard dependency for everything after it). Building the SDK shape now, isolated from any business logic, means later phases (Merchant Creation, Store Capabilities, Payments, etc.) only need to add domain methods to an already-tested, already-correct HTTP foundation.
- **Decision:** Recorded as [ADR-018](08_ARCHITECTURE_DECISIONS.md#adr-018--surfboard-sdk-implementation-choices-phase-2): native `fetch` (no new HTTP dependency — Node ≥18 provides it), exponential-backoff retry (200ms × 2^attempt, 2 retries, retryable-status-aware), `AbortController`-based timeout (10s default), and two **explicit placeholders** — the auth-header scheme and the webhook HMAC-SHA256 signature scheme — both isolated to one file each specifically so confirming Surfboard's real scheme later is a one-file change. Verified the whole request pipeline live (not just via Vitest) with a real Express app + mocked `fetch`: success, retry-then-succeed, non-retryable 4xx, and timeout all produced the correct `SurfboardApiError`/502/`SURFBOARD_ERROR` envelope through the real `error.middleware.js`. One Vitest-specific gotcha surfaced and was worked around: asserting `error instanceof SurfboardApiError` across an ESM-import/CJS-require module boundary intermittently failed under Vitest's module graph (confirmed via a live Node script that the real runtime behavior is correct) — tests were rewritten to assert on `.name`/`.code`/`.message` instead, which is more robust regardless of this environment quirk. No file under `modules/`, `controllers/`, `routes/`, or `firebase/` was touched — confirmed via `git status` before finishing.

### 2026-07-29 — Surfboard-alignment documentation realignment (Lead Architect pass)

- **Prompt Summary:** User (acting as Lead Software Architect) instructed a full stop on feature development and a complete documentation review/correction: after researching the Surfboard Developer Portal, the team discovered Surfboard is the system of record for Merchant, Store, Device, Payment, Branding, Tips, and Payment Methods — not just a payment processor sitting behind a SurfPOS-owned merchant/store/payment schema as earlier docs assumed. Every doc referencing the old architecture was to be rewritten or corrected; four new documents were to be created; no application code was to be touched.
- **Files Changed:** Full rewrites: `docs/01_PROJECT_OVERVIEW.md`, `docs/02_ARCHITECTURE.md`, `docs/03_DATABASE_DESIGN.md`, `docs/04_API_DOCUMENTATION.md`, `docs/05_FEATURES.md`, `docs/07_CODING_RULES.md`, `docs/08_ARCHITECTURE_DECISIONS.md`, `docs/10_TASKS.md`, `docs/12_README.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/15_SURFBOARD_INTEGRATION.md`, `docs/17_FOLDER_STRUCTURE.md`. Updated (lighter touch): `docs/06_UI_UX_GUIDE.md`, `docs/09_PROMPT_HISTORY.md` (this entry), `docs/11_CHANGELOG.md`, `docs/14_DEVELOPER_GUIDE.md`, `docs/16_AI_MODULE.md`, `docs/18_CONTRIBUTING.md`. New: `docs/19_SURFBOARD_WORKFLOWS.md`, `docs/20_DOMAIN_MODEL.md`, `docs/21_BACKEND_GUIDELINES.md`, `docs/22_DEVELOPMENT_ROADMAP.md`. Also synced: `.claude/{project,projectStatus,decision,memory}.md`, root `README.md`. **No file under `backend/src/`, `backend/tests/`, `.github/workflows/`, `.husky/`, or any application-code path was touched** — verified, since Phase 1 (the only code that exists) never implemented merchant/store/payment persistence, so there was no code fallout from this correction.
- **Reason:** The previous architecture assumed Firebase would own `merchants/{merchantId}`, `stores/{storeId}`, and `payments/{paymentId}` as SurfPOS-side records, with Surfboard referenced only via a `surfboardMerchantId`-style field. Building against that assumption would have created two independent systems tracking the same business objects (merchant profile, store, payment status) — exactly the "disconnected payments and POS" fragmentation problem SurfPOS AI exists to solve, just recreated one layer down. Correcting this before any business-domain code exists avoids a costly schema migration later.
- **Decision:** Recorded as [ADR-014](08_ARCHITECTURE_DECISIONS.md#adr-014--surfboard-is-the-system-of-record-for-merchant-store-device-payment-branding-tips-and-payment-methods) (Surfboard is the system of record for the seven entities), [ADR-015](08_ARCHITECTURE_DECISIONS.md#adr-015--repository--mapper-pattern-formalized) (Repository + Mapper layers formalized), [ADR-016](08_ARCHITECTURE_DECISIONS.md#adr-016--surfboard-domain-module-split) (one module per Surfboard entity, Tips/Payment Methods folded into Payments/Store rather than separate modules), and [ADR-017](08_ARCHITECTURE_DECISIONS.md#adr-017--surfboard-payments-as-the-exclusive-payment-integration) (formalizing the founding Surfboard-exclusivity decision that previously only existed as `.claude/decision.md` D-009, never a proper ADR). [ADR-002](08_ARCHITECTURE_DECISIONS.md#adr-002--why-firebase-auth-realtime-database-storage-for-application-data) and [ADR-008](08_ARCHITECTURE_DECISIONS.md#adr-008--multi-tenant-data-model-from-day-one-multi-store-ui-deferred) were amended in place (not deleted) with a note narrowing their scope, per this file's own "never edit history away" rule. A new open item was surfaced rather than silently resolved: removing direct Flutter↔Firebase access (implied by the user's new architecture diagram, which shows only Flutter → Express Backend) removes the real-time RTDB listener mechanism the original dashboard/sale-status design leaned on — logged as a new pending item under [ADR-009](08_ARCHITECTURE_DECISIONS.md#adr-009--pending-decisions-to-record-here-once-made), not invented a solution for. The entire roadmap was replaced with the user's explicit 13-phase order in [22_DEVELOPMENT_ROADMAP.md](22_DEVELOPMENT_ROADMAP.md); [10_TASKS.md](10_TASKS.md) was rewritten to track granular status against those same phase numbers. A new `Supplier` entity was formalized (`suppliers/{merchantId}/{supplierId}`) to replace a previously free-text `supplierName` field, since [20_DOMAIN_MODEL.md](20_DOMAIN_MODEL.md) explicitly required it. As an incidental side effect of fully rewriting `01_PROJECT_OVERVIEW.md` and `03_DATABASE_DESIGN.md`, their stale India/INR example values were corrected to Sweden/SEK/`sv-SE` (per the already-accepted, previously-unpropagated `D-010` decision) — not an exhaustive propagation pass, just a fix encountered while those specific files were already being rewritten wholesale.

### 2026-07-29 — Backend infrastructure hardening (pre-business-logic)

- **Prompt Summary:** User asked to continue past the Phase 1 foundational scaffolding (task 0.7) with backend *infrastructure* hardening — lint/format tooling, git hooks, compression + rate limiting, a proper `constants/` layer, JSDoc `types/`, placeholder Surfboard integration clients, richer per-request logging, a real test suite, a Swagger folder skeleton, and CI — explicitly deferring authentication and business modules to a later, separately-approved pass.
- **Files Changed:** `backend/package.json` (new deps: `compression`, `express-rate-limit`; new devDeps: `eslint`, `@eslint/js`, `globals`, `eslint-config-prettier`, `prettier`, `vitest`, `supertest`; new scripts: `build`, `test`, `test:watch`, `lint`, `lint:fix`, `format`, `format:check`); new `backend/eslint.config.js`, `backend/.prettierrc.json`, `backend/.prettierignore`; new `backend/src/constants/{httpStatus,errorCodes,messages,roles,permissions,apiRoutes,apiVersion,regex,environmentKeys,index}.js` (and refactored `utils/errors.js`, `utils/response.js`, `middleware/error.middleware.js`, `middleware/auth.middleware.js`, `config/index.js`, `app.js` to use them instead of inline literals); new `backend/src/types/{README,user.types,response.types,error.types,express.types}.js` (JSDoc-only); new `backend/src/integrations/{README,surfboard/{surfboardClient.base,auth.client,merchant.client,payment.client,store.client,device.client,branding.client,index}.js}`; new `backend/src/middleware/rateLimit.middleware.js`; `backend/src/app.js` updated (compression, rate limiter mounted after `/health`, pino-http reconfigured for full per-request logging); new `backend/src/docs/swagger/{README.md,paths/.gitkeep,components/.gitkeep,schemas/.gitkeep}`; new `backend/vitest.config.mjs`, `backend/tests/{health,notFound,response}.test.js`; new root `.husky/pre-commit`, root `package.json` (`prepare` script, `husky` devDep); new `.github/workflows/backend.yml`. Docs: `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-012, ADR-013), `.claude/decision.md` (D-014, D-015), `docs/17_FOLDER_STRUCTURE.md` (`integrations/`, `types/`, `docs/swagger/` added to the tree + new "integrations/ vs modules/" section), `backend/src/modules/surfboard/README.md`, `backend/tests/README.md`, `.github/workflows/README.md`, `backend/README.md`.
- **Reason:** The user wants the backend production-hardened (consistent style, automated checks, observability, a documented layering for third-party clients) before any business-domain module is built on top of it, so those modules inherit good conventions from day one instead of retrofitting them later.
- **Decision:** Two new architectural decisions were needed and are recorded in [ADR-012](08_ARCHITECTURE_DECISIONS.md#adr-012--lintformat-tooling-eslint-flat-config--prettier-not-airbnb-base)/[D-014](../.claude/decision.md#d-014--lintformat-tooling-eslint-flat-config--prettier-not-airbnb-base) (ESLint flat config + a lean custom ruleset instead of Airbnb-base — chosen directly rather than asked, since it's a low-stakes, easily-reversible tooling choice explicitly assigned as an implementation task, unlike the zod/pino decisions in the previous session which the project's own docs had explicitly flagged as open questions for the user) and [ADR-013](08_ARCHITECTURE_DECISIONS.md#adr-013--srcintegrations-vs-srcmodules-split)/[D-015](../.claude/decision.md#d-015--srcintegrations-vs-srcmodules-split) (`src/integrations/<provider>/` for raw third-party HTTP clients vs. `src/modules/<provider>/` for the business/orchestration logic that calls them — needed because the requested `src/integrations/surfboard/` would otherwise duplicate the already-reserved `src/modules/surfboard/`). `GET /health` was deliberately kept outside the new global rate limiter (mounted before it in `app.js`) so infra liveness probes are never throttled. Surfboard client files are singleton instances of a shared `SurfboardBaseClient` whose `request()` method intentionally throws — real HTTP calls are out of scope until the Surfboard API surface is confirmed (`ADR-009`). Permission/role constants are defined but not yet enforced by any middleware, since authorization logic is explicitly deferred to the next (approved) phase.

### 2026-07-29 — Backend Phase 1 foundational scaffolding

- **Prompt Summary:** User (acting as senior backend engineer persona) requested the Phase 1 backend foundation be implemented: project init, Express server setup, env config, logger, Firebase Admin SDK, auth middleware, global error handler, response helper, validation middleware, and a health check endpoint — the first real application code in the repository, explicitly superseding the earlier documentation-only constraint.
- **Files Changed:** `backend/package.json` (real dependencies: `express`, `firebase-admin`, `zod`, `pino`, `pino-http`, `helmet`, `cors`, `dotenv`; devDependency `nodemon`, `pino-pretty`); new `backend/src/config/index.js`, `backend/src/utils/{logger,errors,response,asyncHandler}.js`, `backend/src/firebase/admin.js`, `backend/src/middleware/{auth,validate,error}.middleware.js`, `backend/src/{routes,controllers,services}/health.*`; rewrote `backend/src/app.js` and `backend/src/server.js` from placeholders into real implementations; `backend/.env.example` (added `LOG_LEVEL`, `CORS_ALLOWED_ORIGINS`). Docs: `docs/08_ARCHITECTURE_DECISIONS.md` (ADR-010, ADR-011), `.claude/decision.md` (D-012, D-013), `docs/04_API_DOCUMENTATION.md` (§ 13 Health & Infra), `docs/10_TASKS.md` (task 0.7 added, 0.2 updated), `docs/13_CLAUDE_CONTEXT.md`, `docs/11_CHANGELOG.md`, `.claude/projectStatus.md`, `.claude/memory.md`.
- **Reason:** Phase 1 (per [10_TASKS.md](10_TASKS.md)) cannot start on any real feature (auth, merchant, inventory, etc.) without a working Express app, centralized error handling, a validation pattern, and a Firebase Admin SDK entry point — this is the shared foundation every subsequent module builds on, per [13_CLAUDE_CONTEXT.md § 11](13_CLAUDE_CONTEXT.md#11-how-to-continue-development).
- **Decision:** Resolved two of the five items in [ADR-009](08_ARCHITECTURE_DECISIONS.md#adr-009--pending-decisions-to-record-here-once-made) by asking the user directly (they were explicitly logged as open questions for the project owner in `.claude/memory.md`, not something to pick silently): backend validation library is **`zod`** ([ADR-010](08_ARCHITECTURE_DECISIONS.md#adr-010--backend-validation-library-zod)/[D-012](../.claude/decision.md#d-012--backend-validation-library-zod)), backend logging library is **`pino`** ([ADR-011](08_ARCHITECTURE_DECISIONS.md#adr-011--backend-logging-library-pino)/[D-013](../.claude/decision.md#d-013--backend-logging-library-pino)). OCR provider and production font remain open (not needed for this work). Firebase Admin SDK initialization is deliberately **lazy** — the Firebase project isn't provisioned yet (task 0.3), so requiring `firebase/admin.js` or booting the server must not crash; only an actual `getAuth()`/`getDb()`/`getStorageBucket()` call without credentials throws, surfaced as a normal 500 by `error.middleware.js`. `auth.middleware.js` in this pass only verifies the ID token and attaches `{uid, email, phoneNumber}` to `req.user` — it does not yet look up the user's `merchantId`/`role` from `users/{uid}` (that requires the not-yet-built users/merchant read path and lands with task 1.1/1.2), so per-resource `merchantId`/`role` ownership checks remain a controller-level responsibility to add as each resource module is built, not something this pass could fully close out. `firebase-admin` was pinned to `^14.2.0` (latest) instead of an older 12.x line to avoid an easily-avoidable transitive `uuid` advisory; a handful of remaining moderate/high `npm audit` findings live entirely inside `firebase-admin`'s own Google Cloud client dependency tree (`google-gax`/`gaxios`/`gcp-metadata`) and aren't fixable from this repo without a worse downgrade — tracked as a known issue, not silently ignored.

### 2026-07-29 — Diagnose Smart App Control, agree WSL2 path, build Login screen

- **Prompt Summary:** Three chained requests: (1) `flutter run` still failed after the v1-embedding fix, now with the shader-compiler Application Control block reaching the user directly — asked to "solve this and run"; (2) after full diagnosis, user chose "set up Flutter inside WSL2" from three presented options; (3) after confirming WSL2 wasn't actually installed yet (user hadn't run the install step), user said "continue," clarified as "keep going with other work," so work resumed on the standing next task: Screen 2 (Login).
- **Files Changed:** Diagnosed root cause (no files changed for this part — read-only registry/WDAC checks, confirmed via Flutter SDK source that shader compilation is unconditional per-platform). Attempted `wsl --install` guidance (blocked on user needing to elevate + restart — not done by end of session). Added `frontend/lib/features/authentication/presentation/screens/login_screen.dart` (Login screen) and wired it into `frontend/lib/app/app.dart` (Splash → Login via plain `Navigator.pushReplacement`). Added `frontend/test/features/authentication/presentation/screens/login_screen_test.dart` (6 tests) and reorganized `frontend/test/` to mirror `lib/features/` (moved Splash tests into `test/features/authentication/presentation/screens/splash_screen_test.dart`, slimmed `test/widget_test.dart` to the app-shell smoke test only). Replaced the broken `lucide_icons` dependency with `lucide_icons_flutter` across all 7 files that used it. Updated `.claude/projectStatus.md`, `.claude/memory.md`, `.claude/decision.md` (amended D-012).
- **Reason:** The v1-embedding fix was necessary but not sufficient — the same pre-existing shader-compiler block (previously seen only on `flutter build web`) also affects `flutter run`/`flutter build apk`, since it's unconditional across every Flutter target platform. Diagnosing the exact mechanism (Smart App Control, not a corporate WDAC policy) mattered because the two have very different remedies and risk profiles — presenting that clearly let the user make an informed, non-reversible-by-accident choice rather than me picking one unilaterally.
- **Decision:** No new numbered decision for the Smart App Control diagnosis itself (a fact, not a choice) — recorded in `.claude/projectStatus.md § Known Issues #5/#7`. **Real, unplanned bug found and fixed:** the `lucide_icons` package (0.257.0) is unmaintained (its own SDK constraint is pre-Dart-3) and fails to compile against the current Flutter SDK because `IconData` is now a `final class` — caught by `flutter test`'s actual compilation, *not* by `flutter analyze`, which passed clean throughout. Replaced with `lucide_icons_flutter ^3.1.15` (verified same class/icon names, no other code changes needed) — recorded as an amendment to `.claude/decision.md` D-012. Also found and fixed three genuine test bugs while getting Login's test suite green: a test tapping a text label that doesn't exist during a loading state (button label is replaced by a spinner), a test's expected error string colliding with a field's hint text, and a real timer leak in the app-shell test (app.dart's now-real `onReady` schedules a delayed navigation the test didn't settle past) — plus the discovery that tests must use `AppTheme.light` (not a bare `MaterialApp`) to avoid spuriously hitting the same shader-compiler issue via Material 3's default `InkSparkle` ripple, which the app itself deliberately avoids via a custom `splashFactory`. **Final verification for this session:** `flutter analyze` clean throughout; `flutter test` reached **9/9 passing** on a real run after all fixes; `flutter build apk --debug` outcome recorded in the changelog/final report for this session (check there rather than assuming).

### 2026-07-29 — Fix Android v1-embedding error (scaffold android/ platform)

- **Prompt Summary:** User ran `flutter run` from `frontend/` on a connected physical Android device and got "Build failed due to use of deleted Android v1 embedding" — asked to solve it.
- **Files Changed:** Scaffolded `frontend/android/` via `flutter create --platforms=android .` (full Kotlin/Gradle-Kotlin-DSL v2-embedding project — previously just a placeholder README). Updated `frontend/android/README.md` and `frontend/web/README.md` (both had stale "still a placeholder" notes; `web/` was scaffolded earlier but never had its README updated). Corrected the `frontend/.gitignore` comment referring to `android/` as still-placeholder. Updated `.claude/projectStatus.md` (Known Issues #5 broadened — see Decision below).
- **Reason:** `android/` had never actually been generated (per `docs/17_FOLDER_STRUCTURE.md`, platform folders stay placeholders until targeted) — the moment the user connected a real Android device and tried to run on it, that placeholder state became a real blocker. The "v1 embedding" message is Flutter's generic error for an Android project missing the v2 `flutterEmbedding` manifest marker entirely, which is exactly what an empty `android/` folder produces.
- **Decision:** No new formal `decision.md` entry — this is a mechanical fix (running a documented, already-planned scaffolding step), not a new architectural choice. **Verification:** `flutter analyze` clean (`No issues found!`, no Dart files changed by this fix). `flutter build apk --debug` was run to confirm the fix end-to-end: the v1-embedding error is gone (confirms the fix), but the build now fails at a later stage on the *same* `impellerc.exe` Application Control block already known from web builds (see previous log entry) — now confirmed to affect Android builds too, not just web (4/4 build attempts across web-release, web-debug, and android-debug have all failed identically on this one binary). `flutter test` passed 3/3 again on this pass (2 passes, 1 fail across all attempts today — see `.claude/projectStatus.md § Known Issues #5` for the full, still-not-fully-understood pattern). Reported to the user precisely: original error fixed, but a pre-existing environment restriction prevents confirming a real device install from this machine.

### 2026-07-29 — Mandatory verification workflow established + const-lint cleanup

- **Prompt Summary:** User mandated a strict, repeatable workflow for every future task: format → `flutter analyze` (must reach exactly "No issues found!", including INFO-level lints) → `flutter test` → a build check, then a fixed-format report — with explicit instructions to never claim a test/build passed without it actually running successfully, and to never call a task "done" while any analyzer issue (including const-lint infos) remains. Also asked to apply it immediately to the current codebase.
- **Files Changed:** `frontend/lib/core/widgets/loading/skeleton_list.dart` (added `const` in 7 places per `prefer_const_constructors`/`prefer_const_literals_to_create_immutables` — no UI/behavior change, purely compile-time). 9 other files reformatted by `dart format .` (whitespace/line-wrap only). Added `.claude/workflow.md § 14` (Mandatory Verification Sequence) and `.claude/commands.md § Verify & Report` (the exact report template). Updated `docs/07_CODING_RULES.md § 1` to point to the mandatory sequence and require zero issues of any severity, not just zero warnings. Updated `.claude/projectStatus.md` (Known Issues #5 revised with the more nuanced test/build finding below) and `.claude/memory.md`.
- **Reason:** Establish an enforceable, repeatable quality gate so future sessions can't silently skip verification or report unconfirmed results as fact — directly motivated by this session's own earlier mistake of reporting "flutter test (3 passing widget tests)" before the test run had actually completed (corrected in the previous log entry).
- **Decision:** No new `decision.md` entry (this is a process/workflow rule, not an architectural decision) — recorded instead in `.claude/workflow.md` and `.claude/commands.md` directly, since those are the process-owning files. **Verification result, run twice for reliability:** `flutter analyze` is clean (`No issues found!`) and has been reliable across every run. `flutter test` passed 3/3 on this pass (previously failed once on the same environment issue below — see Known Issues #5 for the full nuance). `flutter build web` (release) failed identically on both attempts with `ProcessException: An Application Control policy has blocked this file ... impellerc.exe` — a Windows security policy blocking Flutter's own shader-compiler binary during release asset-bundle building (precompiling Material's `ink_sparkle` ripple shader), confirmed to be a machine/security-policy restriction (the binary is present and undamaged on disk), not a code defect, and not something to work around by disabling security software.

### 2026-07-29 — .claude/ knowledge base, then premium Flutter design system + Splash screen

- **Prompt Summary:** Two related requests: (1) create a `.claude/` Claude-specific knowledge base (`project.md`, `decision.md`, `projectStatus.md`, `workflow.md`, `commands.md`, `memory.md`) so future sessions don't need to read the whole codebase; (2) acting as a senior Flutter UI/UX engineer, build a premium, non-generic design system (theme, colors, typography, spacing, reusable buttons/cards/text fields/dialogs/bottom sheets/loading/empty states/app bars/navigation) for a 26-screen POS app, then implement screens one at a time starting with Splash — explicitly no backend/business logic, and explicitly not all 26 screens in one pass.
- **Files Changed:** Added `.claude/*.md` (6 files). Added `frontend/lib/app/themes/{app_colors,app_typography,app_spacing,app_shadows,app_theme}.dart`. Added `frontend/lib/core/widgets/{buttons,cards,text_fields,dialogs,bottom_sheets,loading,empty_states,app_bars,navigation,branding}/*.dart` (23 files). Added `frontend/lib/features/authentication/presentation/screens/splash_screen.dart`. Rewrote `frontend/lib/app/app.dart` and `frontend/lib/main.dart` to actually wire up `MaterialApp`/`AppTheme`/`SplashScreen`. Downloaded the official Surfboard Payments logo to `frontend/assets/logos/surfboard-payments-white-icon.svg`. Updated `frontend/pubspec.yaml` (real dependencies, assets entry). Scaffolded the `web/` Flutter platform runner (via `flutter create --platforms=web .`) to actually build/test the app; reverted incidental `GeneratedPluginRegistrant`/ephemeral files that the same command dropped into the still-placeholder `android/`, `ios/`, `linux/`, `macos/` folders, since only `web/` was intended. Replaced the default counter `frontend/test/widget_test.dart` with real Splash-screen smoke tests. Updated `.claude/projectStatus.md`, `.claude/decision.md` (new entries D-012, D-013), `.claude/memory.md`.
- **Reason:** The `.claude/` files give any future session/developer a fast, code-independent orientation. The design-system-first approach was explicitly required by the user ("Only after the design system is complete should you begin implementing the UI") so all 26 screens share one consistent, custom (non-Material-default) visual language instead of each screen inventing its own styling.
- **Decision:** Logged as `.claude/decision.md` D-012 (UI dependency choices: `google_fonts`, `flutter_svg`, `lucide_icons` — versions resolved live via `flutter pub add`, not guessed) and D-013 (floating, non-notched FAB for the 5-item bottom nav, since the brief's 5 destinations don't split evenly around a Material notch built for 4). **Verification performed:** `flutter analyze` passed cleanly (0 issues across the whole `lib/` tree) — this is the only verification tool that actually works on this machine. Both `flutter test` and `flutter build web --release` were attempted and **both failed identically**, not from any code defect but because a Windows Application Control policy on this machine blocks execution of Flutter's own `impellerc.exe` shader-compiler binary (used to precompile Material's `ink_sparkle` ripple shader during asset-bundle building — see `.claude/projectStatus.md § Known Issues #5`). This blocks runtime/test verification on this machine entirely until that policy is adjusted or verification runs elsewhere; do not report tests as "passing" here without an actual successful run confirming it. Given the 26-screen scope, only Splash was implemented this session; the remaining 25 screens are explicitly deferred to future turns, one at a time, per the user's own stated process and this project's established pattern of pausing for review at natural checkpoints.

### 2026-07-29 — Enterprise repository folder structure scaffold

- **Prompt Summary:** User requested the complete project folder structure be created before any application code — folders, placeholder files, and README.md files only, following a professional enterprise architecture, with an explicit tree spanning `frontend/`, `backend/`, `firebase/`, `scripts/`, `api-testing/`, `design/`, `.github/`, `.vscode/`, and root-level config files.
- **Files Changed:** Created the full directory tree (87 folders) with placeholder `README.md`/`.gitkeep` files and stub entry points (`main.dart`, `app.dart`, `app.js`, `server.js` — comments only, no logic); placeholder manifests (`pubspec.yaml`, both `package.json` files, `.env.example`); placeholder Firebase config (`firebase.json`, `database.rules.json`, `storage.rules`, `indexes.json`); root `.gitignore`, `LICENSE` (unchosen, left as a decision for the project owner), `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`. Updated `docs/17_FOLDER_STRUCTURE.md` to match the actual scaffold and fixed stale `mobile/` → `frontend/` references in `docs/10_TASKS.md`, `docs/12_README.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/14_DEVELOPER_GUIDE.md`, `docs/15_SURFBOARD_INTEGRATION.md`, `docs/06_UI_UX_GUIDE.md`, and `docs/07_CODING_RULES.md`.
- **Reason:** Establish a navigable, professional repository skeleton so implementation work (Phase 1 onward per [10_TASKS.md](10_TASKS.md)) has an agreed folder for every concern before any code is written, consistent with the "documentation and structure before implementation" approach set at project start.
- **Decision:** The user's provided tree used `frontend/` (not the `mobile/` name assumed in the original `08_ARCHITECTURE_DECISIONS.md`/`17_FOLDER_STRUCTURE.md` draft) and introduced two feature folders not previously specified in `05_FEATURES.md` — `merchant/` (business profile, split out from the original combined Merchant Registration + Settings scope) and `profile/` (the signed-in user's own account profile, split out from Authentication). Folder names `barcode_scanner/`/`invoice_scanner/` were renamed to `barcode/`/`invoice_ai/`. No new feature *behavior* was defined — only folder scaffolding and cross-reference updates. The per-feature internal `data/domain/presentation` split documented in `07_CODING_RULES.md` was **not** scaffolded inside each feature folder yet — it is added feature-by-feature as each is actually implemented, to avoid speculative empty structure. The Node backend's Security Rules and Firebase config were placed in a dedicated top-level `firebase/` folder (matching the user's tree) rather than inside `backend/`, which supersedes the original `17_FOLDER_STRUCTURE.md` draft that had `database.rules.json` living under `backend/`.

### 2026-07-29 — Initial project documentation system

- **Prompt Summary:** User requested a complete documentation-only pass before any application code is written: full architecture, database design, API reference, feature specs, UI/UX guide, coding rules, ADRs, and process docs (18 files total) inside `/docs`, so a future developer or Claude session can understand the entire project without reading a codebase (since no codebase exists yet).
- **Files Changed:** Created `docs/01_PROJECT_OVERVIEW.md` through `docs/18_CONTRIBUTING.md` (all 18 documentation files). No application code was created, per explicit instruction.
- **Reason:** Establish a project knowledge base up front, before implementation begins, so architectural decisions (tech stack, data model, API shape, coding rules) are made deliberately once rather than discovered ad hoc during coding.
- **Decision:** Established the founding architecture decisions in [08_ARCHITECTURE_DECISIONS.md](08_ARCHITECTURE_DECISIONS.md) (ADR-001 through ADR-008): Flutter frontend, Firebase-only data platform, Node/Express backend, AI-assisted invoice scanning with mandatory human review, smartphone-only hardware model, cloud-only (non-offline-first) architecture, Riverpod state management (proposed, pending confirmation), and a multi-tenant-from-day-one schema with multi-store UI deferred. Several decisions were explicitly left open and logged as pending in ADR-009 (OCR provider, validation/logging libraries, exact Surfboard API surface, production font, insights storage location) — these must be resolved and logged as new ADR entries before or during Phase 1 implementation.

---

**Next:** [10_TASKS.md](10_TASKS.md) — the project roadmap this documentation feeds into.
