# 09 — Prompt History

> This file is Claude's memory of *why* the codebase looks the way it does, across sessions. **Every future Claude Code session that makes a non-trivial change must append an entry here** before finishing. Read alongside [13_CLAUDE_CONTEXT.md](13_CLAUDE_CONTEXT.md) and [11_CHANGELOG.md](11_CHANGELOG.md) (this file explains the *why*; the changelog records the *what*, user-facing).

---

## How to Add an Entry

Append a new entry at the **top** of the log (§2), directly under this heading, using the template below. Do not edit or delete past entries — this is a historical record.

```markdown
### YYYY-MM-DD — <short title>

- **Prompt Summary:** What the user asked for, in 1–3 sentences.
- **Files Changed:** List of files/folders touched.
- **Reason:** Why this work was requested (business/technical motivation).
- **Decision:** Any decision made while doing the work (link a new/updated ADR in [08_ARCHITECTURE_DECISIONS.md](08_ARCHITECTURE_DECISIONS.md) if applicable).
```

---

## Log

### 2026-07-29 — Diagnose Smart App Control, agree WSL2 path, build Login screen

- **Prompt Summary:** Three chained requests: (1) `flutter run` still failed after the v1-embedding fix, now with the shader-compiler Application Control block reaching the user directly — asked to "solve this and run"; (2) after full diagnosis, user chose "set up Flutter inside WSL2" from three presented options; (3) after confirming WSL2 wasn't actually installed yet (user hadn't run the install step), user said "continue," clarified as "keep going with other work," so work resumed on the standing next task: Screen 2 (Login).
- **Files Changed:** Diagnosed root cause (no files changed for this part — read-only registry/WDAC checks, confirmed via Flutter SDK source that shader compilation is unconditional per-platform). Attempted `wsl --install` guidance (blocked on user needing to elevate + restart — not done by end of session). Added `frontend/lib/features/authentication/presentation/screens/login_screen.dart` (Login screen) and wired it into `frontend/lib/app/app.dart` (Splash → Login via plain `Navigator.pushReplacement`). Added `frontend/test/features/authentication/presentation/screens/login_screen_test.dart` (6 tests) and reorganized `frontend/test/` to mirror `lib/features/` (moved Splash tests into `test/features/authentication/presentation/screens/splash_screen_test.dart`, slimmed `test/widget_test.dart` to the app-shell smoke test only). Replaced the broken `lucide_icons` dependency with `lucide_icons_flutter` across all 7 files that used it. Updated `.claude/projectStatus.md`, `.claude/memory.md`, `.claude/decision.md` (amended D-012).
- **Reason:** The v1-embedding fix was necessary but not sufficient — the same pre-existing shader-compiler block (previously seen only on `flutter build web`) also affects `flutter run`/`flutter build apk`, since it's unconditional across every Flutter target platform. Diagnosing the exact mechanism (Smart App Control, not a corporate WDAC policy) mattered because the two have very different remedies and risk profiles — presenting that clearly let the user make an informed, non-reversible-by-accident choice rather than me picking one unilaterally.
- **Decision:** No new numbered decision for the Smart App Control diagnosis itself (a fact, not a choice) — recorded in `.claude/projectStatus.md § Known Issues #5/#7`. **Real, unplanned bug found and fixed:** the `lucide_icons` package (0.257.0) is unmaintained (its own SDK constraint is pre-Dart-3) and fails to compile against the current Flutter SDK because `IconData` is now a `final class` — caught by `flutter test`'s actual compilation, *not* by `flutter analyze`, which passed clean throughout. Replaced with `lucide_icons_flutter ^3.1.15` (verified same class/icon names, no other code changes needed) — recorded as an amendment to `.claude/decision.md` D-012. Also found and fixed three genuine test bugs while getting Login's test suite green: a test tapping a text label that doesn't exist during a loading state (button label is replaced by a spinner), a test's expected error string colliding with a field's hint text, and a real timer leak in the app-shell test (app.dart's now-real `onReady` schedules a delayed navigation the test didn't settle past) — plus the discovery that tests must use `AppTheme.light` (not a bare `MaterialApp`) to avoid spuriously hitting the same shader-compiler issue via Material 3's default `InkSparkle` ripple, which the app itself deliberately avoids via a custom `splashFactory`. **Final verification for this session:** `flutter analyze` clean throughout; `flutter test` reached **9/9 passing** on a real run after all fixes; `flutter build apk --debug` outcome recorded in the changelog/final report for this session (check there rather than assuming).

### 2026-07-29 — Fix Android v1-embedding error (scaffold android/ platform)

- **Prompt Summary:** User ran `flutter run` from `frontend/` on a connected physical Android device and got "Build failed due to use of deleted Android v1 embedding" — asked to solve it.
- **Files Changed:** Scaffolded `frontend/android/` via `flutter create --platforms=android .` (full Kotlin/Gradle-Kotlin-DSL v2-embedding project — previously just a placeholder README). Updated `frontend/android/README.md` and `frontend/web/README.md` (both had stale "still a placeholder" notes; `web/` was scaffolded earlier but never had its README updated). Corrected the `frontend/.gitignore` comment referring to `android/` as still-placeholder. Updated `.claude/projectStatus.md` (Known Issues #5 broadened — see Decision below).
- **Reason:** `android/` had never actually been generated (per `docs/17_FOLDER_STRUCTURE.md`, platform folders stay placeholders until targeted) — the moment the user connected a real Android device and tried to run on it, that placeholder state became a real blocker. The "v1 embedding" message is Flutter's generic error for an Android project missing the v2 `flutterEmbedding` manifest marker entirely, which is exactly what an empty `android/` folder produces.
- **Decision:** No new formal `decision.md` entry — this is a mechanical fix (running a documented, already-planned scaffolding step), not a new architectural choice. **Verification:** `flutter analyze` clean (`No issues found!`, no Dart files changed by this fix). `flutter build apk --debug` was run to confirm the fix end-to-end: the v1-embedding error is gone (confirms the fix), but the build now fails at a later stage on the *same* `impellerc.exe` Application Control block already known from web builds (see previous log entry) — now confirmed to affect Android builds too, not just web (4/4 build attempts across web-release, web-debug, and android-debug have all failed identically on this one binary). `flutter test` passed 3/3 again on this pass (2 passes, 1 fail across all attempts today — see `.claude/projectStatus.md § Known Issues #5` for the full, still-not-fully-understood pattern). Reported to the user precisely: original error fixed, but a pre-existing environment restriction prevents confirming a real device install from this machine.

### 2026-07-29 — Mandatory verification workflow established + const-lint cleanup

- **Prompt Summary:** User mandated a strict, repeatable workflow for every future task: format → `flutter analyze` (must reach exactly "No issues found!", including INFO-level lints) → `flutter test` → a build check, then a fixed-format report — with explicit instructions to never claim a test/build passed without it actually running successfully, and to never call a task "done" while any analyzer issue (including const-lint infos) remains. Also asked to apply it immediately to the current codebase.
- **Files Changed:** `frontend/lib/core/widgets/loading/skeleton_list.dart` (added `const` in 7 places per `prefer_const_constructors`/`prefer_const_literals_to_create_immutables` — no UI/behavior change, purely compile-time). 9 other files reformatted by `dart format .` (whitespace/line-wrap only). Added `.claude/workflow.md § 14` (Mandatory Verification Sequence) and `.claude/commands.md § Verify & Report` (the exact report template). Updated `docs/07_CODING_RULES.md § 1` to point to the mandatory sequence and require zero issues of any severity, not just zero warnings. Updated `.claude/projectStatus.md` (Known Issues #5 revised with the more nuanced test/build finding below) and `.claude/memory.md`.
- **Reason:** Establish an enforceable, repeatable quality gate so future sessions can't silently skip verification or report unconfirmed results as fact — directly motivated by this session's own earlier mistake of reporting "flutter test (3 passing widget tests)" before the test run had actually completed (corrected in the previous log entry).
- **Decision:** No new `decision.md` entry (this is a process/workflow rule, not an architectural decision) — recorded instead in `.claude/workflow.md` and `.claude/commands.md` directly, since those are the process-owning files. **Verification result, run twice for reliability:** `flutter analyze` is clean (`No issues found!`) and has been reliable across every run. `flutter test` passed 3/3 on this pass (previously failed once on the same environment issue below — see Known Issues #5 for the full nuance). `flutter build web` (release) failed identically on both attempts with `ProcessException: An Application Control policy has blocked this file ... impellerc.exe` — a Windows security policy blocking Flutter's own shader-compiler binary during release asset-bundle building (precompiling Material's `ink_sparkle` ripple shader), confirmed to be a machine/security-policy restriction (the binary is present and undamaged on disk), not a code defect, and not something to work around by disabling security software.

### 2026-07-29 — .claude/ knowledge base, then premium Flutter design system + Splash screen

- **Prompt Summary:** Two related requests: (1) create a `.claude/` Claude-specific knowledge base (`project.md`, `decision.md`, `projectStatus.md`, `workflow.md`, `commands.md`, `memory.md`) so future sessions don't need to read the whole codebase; (2) acting as a senior Flutter UI/UX engineer, build a premium, non-generic design system (theme, colors, typography, spacing, reusable buttons/cards/text fields/dialogs/bottom sheets/loading/empty states/app bars/navigation) for a 26-screen POS app, then implement screens one at a time starting with Splash — explicitly no backend/business logic, and explicitly not all 26 screens in one pass.
- **Files Changed:** Added `.claude/*.md` (6 files). Added `frontend/lib/app/themes/{app_colors,app_typography,app_spacing,app_shadows,app_theme}.dart`. Added `frontend/lib/core/widgets/{buttons,cards,text_fields,dialogs,bottom_sheets,loading,empty_states,app_bars,navigation,branding}/*.dart` (23 files). Added `frontend/lib/features/authentication/presentation/screens/splash_screen.dart`. Rewrote `frontend/lib/app/app.dart` and `frontend/lib/main.dart` to actually wire up `MaterialApp`/`AppTheme`/`SplashScreen`. Downloaded the official Surfboard Payments logo to `frontend/assets/logos/surfboard-payments-white-icon.svg`. Updated `frontend/pubspec.yaml` (real dependencies, assets entry). Scaffolded the `web/` Flutter platform runner (via `flutter create --platforms=web .`) to actually build/test the app; reverted incidental `GeneratedPluginRegistrant`/ephemeral files that the same command dropped into the still-placeholder `android/`, `ios/`, `linux/`, `macos/` folders, since only `web/` was intended. Replaced the default counter `frontend/test/widget_test.dart` with real Splash-screen smoke tests. Updated `.claude/projectStatus.md`, `.claude/decision.md` (new entries D-012, D-013), `.claude/memory.md`.
- **Reason:** The `.claude/` files give any future session/developer a fast, code-independent orientation. The design-system-first approach was explicitly required by the user ("Only after the design system is complete should you begin implementing the UI") so all 26 screens share one consistent, custom (non-Material-default) visual language instead of each screen inventing its own styling.
- **Decision:** Logged as `.claude/decision.md` D-012 (UI dependency choices: `google_fonts`, `flutter_svg`, `lucide_icons` — versions resolved live via `flutter pub add`, not guessed) and D-013 (floating, non-notched FAB for the 5-item bottom nav, since the brief's 5 destinations don't split evenly around a Material notch built for 4). **Verification performed:** `flutter analyze` passed cleanly (0 issues across the whole `lib/` tree) — this is the only verification tool that actually works on this machine. Both `flutter test` and `flutter build web --release` were attempted and **both failed identically**, not from any code defect but because a Windows Application Control policy on this machine blocks execution of Flutter's own `impellerc.exe` shader-compiler binary (used to precompile Material's `ink_sparkle` ripple shader during asset-bundle building — see `.claude/projectStatus.md § Known Issues #5`). This blocks runtime/test verification on this machine entirely until that policy is adjusted or verification runs elsewhere; do not report tests as "passing" here without an actual successful run confirming it. Given the 26-screen scope, only Splash was implemented this session; the remaining 25 screens are explicitly deferred to future turns, one at a time, per the user's own stated process and this project's established pattern of pausing for review at natural checkpoints.

### 2026-07-29 — Enterprise repository folder structure scaffold

- **Prompt Summary:** User requested the complete project folder structure be created before any application code — folders, placeholder files, and README.md files only, following a professional enterprise architecture, with an explicit tree spanning `frontend/`, `backend/`, `firebase/`, `scripts/`, `api-testing/`, `design/`, `.github/`, `.vscode/`, and root-level config files.
- **Files Changed:** Created the full directory tree (87 folders) with placeholder `README.md`/`.gitkeep` files and stub entry points (`main.dart`, `app.dart`, `app.js`, `server.js` — comments only, no logic); placeholder manifests (`pubspec.yaml`, both `package.json` files, `.env.example`); placeholder Firebase config (`firebase.json`, `database.rules.json`, `storage.rules`, `indexes.json`); root `.gitignore`, `LICENSE` (unchosen, left as a decision for the project owner), `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`. Updated `docs/17_FOLDER_STRUCTURE.md` to match the actual scaffold and fixed stale `mobile/` → `frontend/` references in `docs/10_TASKS.md`, `docs/12_README.md`, `docs/13_CLAUDE_CONTEXT.md`, `docs/14_DEVELOPER_GUIDE.md`, `docs/15_SURFBOARD_INTEGRATION.md`, `docs/06_UI_UX_GUIDE.md`, and `docs/07_CODING_RULES.md`.
- **Reason:** Establish a navigable, professional repository skeleton so implementation work (Phase 1 onward per [10_TASKS.md](10_TASKS.md)) has an agreed folder for every concern before any code is written, consistent with the "documentation and structure before implementation" approach set at project start.
- **Decision:** The user's provided tree used `frontend/` (not the `mobile/` name assumed in the original `08_ARCHITECTURE_DECISIONS.md`/`17_FOLDER_STRUCTURE.md` draft) and introduced two feature folders not previously specified in `05_FEATURES.md` — `merchant/` (business profile, split out from the original combined Merchant Registration + Settings scope) and `profile/` (the signed-in user's own account profile, split out from Authentication). Folder names `barcode_scanner/`/`invoice_scanner/` were renamed to `barcode/`/`invoice_ai/`. No new feature *behavior* was defined — only folder scaffolding and cross-reference updates. The per-feature internal `data/domain/presentation` split documented in `07_CODING_RULES.md` was **not** scaffolded inside each feature folder yet — it is added feature-by-feature as each is actually implemented, to avoid speculative empty structure. The Node backend's Security Rules and Firebase config were placed in a dedicated top-level `firebase/` folder (matching the user's tree) rather than inside `backend/`, which supersedes the original `17_FOLDER_STRUCTURE.md` draft that had `database.rules.json` living under `backend/`.

### 2026-07-29 — Initial project documentation system

- **Prompt Summary:** User requested a complete documentation-only pass before any application code is written: full architecture, database design, API reference, feature specs, UI/UX guide, coding rules, ADRs, and process docs (18 files total) inside `/docs`, so a future developer or Claude session can understand the entire project without reading a codebase (since no codebase exists yet).
- **Files Changed:** Created `docs/01_PROJECT_OVERVIEW.md` through `docs/18_CONTRIBUTING.md` (all 18 documentation files). No application code was created, per explicit instruction.
- **Reason:** Establish a project knowledge base up front, before implementation begins, so architectural decisions (tech stack, data model, API shape, coding rules) are made deliberately once rather than discovered ad hoc during coding.
- **Decision:** Established the founding architecture decisions in [08_ARCHITECTURE_DECISIONS.md](08_ARCHITECTURE_DECISIONS.md) (ADR-001 through ADR-008): Flutter frontend, Firebase-only data platform, Node/Express backend, AI-assisted invoice scanning with mandatory human review, smartphone-only hardware model, cloud-only (non-offline-first) architecture, Riverpod state management (proposed, pending confirmation), and a multi-tenant-from-day-one schema with multi-store UI deferred. Several decisions were explicitly left open and logged as pending in ADR-009 (OCR provider, validation/logging libraries, exact Surfboard API surface, production font, insights storage location) — these must be resolved and logged as new ADR entries before or during Phase 1 implementation.

---

**Next:** [10_TASKS.md](10_TASKS.md) — the project roadmap this documentation feeds into.
