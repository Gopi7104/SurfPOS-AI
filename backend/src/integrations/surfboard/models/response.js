'use strict';

// JSDoc-only type definitions — see backend/src/types/README.md for the pattern this follows.

/**
 * @typedef {Object} SurfboardResponse
 * @property {number} status
 * @property {Record<string, string>} headers
 * @property {*} data
 * @property {boolean} ok
 */

module.exports = {};
