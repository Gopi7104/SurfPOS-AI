# .github/workflows/

CI/CD pipeline definitions.

- `backend.yml` — install/lint/format-check/test/build for `backend/` on push to `main` and on pull requests touching `backend/**`. No frontend pipeline yet (no Flutter code/tooling exists).
